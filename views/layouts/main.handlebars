<!DOCTYPE html>
<html>
    <head>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Arsenal:wght@400;700&family=Open+Sans:ital,wght@0,300;0,400;1,300&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="/css/styles.css">
        <title>Community Builders</title>
    </head>
<body>
 <header>
            <h1>Community Builders</h1>
            <ul>
                <li><a href="/">Home</a></li>
                <li>
                   <a href="/events">Events</a>
                </li>
                <li>
                    <a href=""> Our Story</a>
                </li>
            {{#if logged_in}}
                <li><a href="/profile">Profile</a></li>
                 <li><a href="/logout">Logout</a></li>
            {{else}}
                <li><a href="/login">Login</a></li>
            {{/if}}
                <li id="headerSignUp">
                    Sign Up
                </li>
            </ul>
        </header>
    <main class="">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
   
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
 <footer>
            <div class='footerCard' id="contact">
                <p id="contactName">
                    Community Builders Incorporated
                </p>
                <p id="contactAddress">
                    123 Volunteer Avenue,<br> Unityville, IL, 98765
                </p>
                <p id=contactNumber>
                    (555) 555 - 5555
                </p>
            </div>
            <div class='footerCard' id="notification">
                <h1>
                    Event Notification Sign Up
                </h1>
                <form id="signUpForm">
                    <label for="signUpSearch"></label>
                    <input type="text" id="signUpSearch" placeholder="Email Address">
                    <button id="signUpButton">Sign Up</button>
                </form>
            </div>
            <div class='footerCard' id="socials">
                <h3>About</h3>
                <h3>Get Involved</h3>
                <div id="socialsLogos">
                    <img class="socialsLogo" src="./Icons/Facebook.png">
                    <img class="socialsLogo" src="./Icons/Instagram.png">
                    <img class="socialsLogo" src="./Icons/Pinterest.png">
                </div>
            </div>
        </footer>
</html>
